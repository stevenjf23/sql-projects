# 🗑️ Sentencia DELETE  

La sentencia `DELETE` en SQL se utiliza para **eliminar registros** de una tabla.  

Permite borrar uno o varios datos según una condición.  

📌 Es importante usarla con `WHERE` para evitar eliminar todos los registros.

En esta sección se encuentra el script .sql donde aplico DELETE.
