# ✏️ Sentencia UPDATE  

La sentencia `UPDATE` en SQL se utiliza para **modificar registros existentes** en una tabla.  

Permite cambiar valores en una o varias columnas según una condición.  

📌 Es importante usarla con `WHERE` para evitar cambios masivos no deseados.

En esta sección se encuentra el script .sql donde aplico UPDATE.
