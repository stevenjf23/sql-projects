# ➕ Sentencia INSERT  

La sentencia `INSERT` en SQL se utiliza para **agregar nuevos registros** a una tabla.  

Permite insertar uno o varios datos en las columnas especificadas.  

📌 Es fundamental para la carga de información en la base de datos.

En esta sección se encuentra el script .sql donde aplico INSERT.
