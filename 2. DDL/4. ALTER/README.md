# 🛠️ Sentencia ALTER  

La sentencia `ALTER` en SQL se utiliza para **modificar la estructura de una tabla existente**.  

Permite agregar, eliminar o cambiar columnas y sus propiedades.  

📌 Es fundamental para actualizar la estructura de la base de datos sin eliminarla.

En esta sección se encuentra el script .sql donde aplico ALTER.
